<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ch.lorenzo.todo</groupId>
  <artifactId>todo-liste</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <name>todo-liste</name>
  <url>http://maven.apache.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <javafx.version>17.0.10</javafx.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-fxml</artifactId>
      <version>${javafx.version}</version>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
          <mainClass>ch.lorenzo.todo.view.TodoMain</mainClass>
          <jvmArgs>
            <jvmArg>-Dprism.allowhidpi=true</jvmArg>
          </jvmArgs>
          <launcher>Todo_Liste</launcher>
          <jlinkImageName>Todo_Liste</jlinkImageName>
          <jlinkZipName>Todo_Liste</jlinkZipName>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.panteleyev</groupId>
        <artifactId>jpackage-maven-plugin</artifactId>
        <version>1.7.1</version>
        <configuration>
          <name>Todo_Liste</name>
          <type>EXE</type>
          <module>ch.lorenzo.todo/ch.lorenzo.todo.view.TodoMain</module>
          <runtimeImage>${project.build.directory}/Todo_Liste</runtimeImage>
          <destination>${project.build.directory}/dist</destination>
          <removeDestination>true</removeDestination>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>21</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
